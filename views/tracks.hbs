<div class = "row">
    {{#each tracksList}}
    <div class = "col-md-6">
        <p>{{this.name}}</p>
    </div>
    <div class = "col-md-6">
        <audio controls src="{{this.preview_url}}"></audio>
    </div>
    {{/each}}
</div>